# name: Deploy to GitHub Pages

# on:
#   push:
#     branches: [ main ]    
#   workflow_dispatch:      

# permissions:
#   contents: read
#   pages: write
#   id-token: write

# jobs:
#   deploy:
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Node
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20   
#           cache: npm
#           cache-dependency-path: Hecathon_Book/twin-book-site/package-lock.json

#       - name: Install dependencies
#         working-directory: Hecathon_Book/twin-book-site
#         run: npm ci
        

#       - name: Build website
#         run: npm run build
#         working-directory: Hecathon_Book/twin-book-site

#       - name: Configure GitHub Pages
#         uses: actions/configure-pages@v4

#       - name: Upload artifact
#         uses: actions/upload-pages-artifact@v3
#         with:
#           path: Hecathon_Book/twin-book-site/build


#       - name: Deploy to GitHub Pages
#         id: deployment
#         uses: actions/deploy-pages@v4


name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      # 👇 Build steps
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20.x
          cache: 'npm'
      - name: Install dependencies
        run: npm ci
      - name: Build
        run: npm run build
      # 👆 Build steps
      - name: Setup Pages
        uses: actions/configure-pages@v4
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # 👇 Specify build output path
          path: build
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4